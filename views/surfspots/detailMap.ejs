<%- include('partials/headMap') %>

    <%- include('partials/navbar.ejs') %> 
    
    <div class="row mb-3"><div  class="col-6 offset-3">
      <div class="card" >
        <!-- <img src="<%=spot.image%>" class="card-img-top" alt="IMG"> -->
        <div class="card-body">
          <h5 class="card-title"><%= spot.spotName %></h5>
          <p class="card-text">Description: <%= spot.info.description %></p>
          <p class="card-text">Atmosphere: <%= spot.info.atmosphere %></p>
          <p class="card-text">General: <%= spot.info.general %></p>
        </div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item"><%= spot.locationStringify() %></li>
          <%if(spot.hasCoordinates){%>
            <li class="list-group-item"><%= spot.coordinates.Lat+"   "+spot.coordinates.Lon%>  </li>
            
    <!--The div element for the map -->
    <div style= "height: 400px;
    width: 100%"id="map"></div>

    <!-- 
     The `defer` attribute causes the callback to execute after the full HTML
     document has been parsed. For non-blocking uses, avoiding race conditions,
     and consistent behavior across browsers, consider loading using Promises
     with https://www.npmjs.com/package/@googlemaps/js-api-loader.
    -->
  
 
    <script
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDyWCoO_MCZNqbpIGB8PmlQo_GckQFLa5k&callback=initMap&v=weekly"
      async
      defer
    ></script>
   
        <% } %> 
          
          <li class="list-group-item"><%= spot.difficulty%></li>
          <li class="list-group-item">Bottom: <%= spot.bottomcomp %></li>
          <li class="list-group-item">Break Type: <%= spot.breaktype%></li>
        </ul>
        <div class="card-body">
          <a href="/surfspots/edit/<%=spot._id%>" class="card-link btn btn-info">Edit</a>
          <form class='d-inline'action="/surfspots/<%=spot._id%>?_method=DELETE" method="post"><button class="card-link btn btn-danger">Delete</button>
          </form>
        </div>
      </div>

    </div></div>
    
      
    </main>
  <
    <%-include('partials/footer')%>
